import { KeyIcon } from "../icons";

export default function Logo() {
  return(
    <div className={`flex justify-center items-center h-10 w-10 bg-white rounded-full  text-cyan-950 `}>
      {KeyIcon}
    </div>
  )
}